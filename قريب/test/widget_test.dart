import 'package:flutter_test/flutter_test.dart';
import 'package:qaryp/main.dart';

void main() {
  testWidgets('QarypApp is defined', (WidgetTester tester) async {
    expect(const QarypApp(), isNotNull);
  });
}
